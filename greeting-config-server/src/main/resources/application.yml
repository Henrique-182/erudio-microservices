server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  profiles:
    active:
    - native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Henrique-182/erudio-config-server
          default-label: main
          search-paths:
          - 'greting-service*'